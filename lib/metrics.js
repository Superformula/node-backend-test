module.exports = {
	increment(label) {
		// aws cloud watch metrics? data dog? statsd?
		// you get the idea...
	},

	record(label, value) {
		// aws cloud watch metrics? data dog? statsd?
		// you get the idea...
	}
};
